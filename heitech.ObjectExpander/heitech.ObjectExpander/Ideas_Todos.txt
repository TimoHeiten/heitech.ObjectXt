// reflection cache for obj

Auto property  with lambda syntax does not work with set.

MappedPropertyManager implements IFormattable
--> returns $"{key}:{value}\n" for all properties.

Config for extender:
	AttributeMap per Typ or global (global is default)
		--> change return type on attributefactory
	SafeInvocation --> no exceptions on Invoke Calls
